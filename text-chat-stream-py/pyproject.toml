[project]
name = "text-chat-stream-langchain-example"
version = "0.1.0"
description = "Python streaming text chat example for an OpenAI-compatible API using LangChain."
requires-python = ">=3.11"
dependencies = [
  "langchain-openai>=1.1.14",
  "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
